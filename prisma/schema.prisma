// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model EventAction {
  action_id String  @unique
  object    String
  name      String
  Event     Event[]
}

model Metadata {
  x_request_id String  @unique
  redirect     String
  description  String
  Event        Event[]
}

model Event {
  id          Int         @id @default(autoincrement())
  event_id    String
  object      String
  actor_id    String
  actor_name  String
  group       String
  action_id   String
  action      EventAction @relation(fields: [action_id], references: [action_id])
  target_id   String
  target_name String
  location    String
  occurred_at String
  metadata_id String
  metadata    Metadata    @relation(fields: [metadata_id], references: [x_request_id])
  created_at   DateTime    @default(now())
  updated_at   DateTime    @updatedAt

  @@index([actor_name])
}
